'use strict';

// Krok 4: nodemailer + HTML šablony

async function sendLeadToFirm(_lead) {
  throw new Error('Mailer ještě není připravený (krok 4)');
}

async function sendConfirmationToClient(_lead) {
  throw new Error('Mailer ještě není připravený (krok 4)');
}

module.exports = { sendLeadToFirm, sendConfirmationToClient };
